import 'package:flutter/material.dart';

class SubmanagerHomePage extends StatelessWidget {
  const SubmanagerHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.red);
  }
}
